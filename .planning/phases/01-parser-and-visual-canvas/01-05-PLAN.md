---
phase: 01-parser-and-visual-canvas
plan: 05
type: execute
wave: 3
depends_on:
  - 01-04
files_modified: []
autonomous: false
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
  - VIS-04
  - PERS-02
  - PERS-03
  - UX-03

must_haves:
  truths:
    - "Complete ER diagram visible in browser with all tables from model.py"
    - "All Phase 1 interactions work: drag, pan, zoom, select, hover, search, collapse, toolbar"
    - "Positions persist across browser reloads via .er.json"
---

<objective>
Visual and functional verification of the complete Phase 1 ER diagram viewer.

Purpose: Plans 01-04 were developed with automated tests for backend logic, but frontend visualization (VIS-01 through VIS-04) and UX interactions require human verification in a real browser.
Output: Confirmation that Phase 1 success criteria are met.
</objective>

<execution_context>
@/Users/simonebrigante/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonebrigante/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-parser-and-visual-canvas/01-CONTEXT.md
@.planning/phases/01-parser-and-visual-canvas/01-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual and functional verification of complete ER diagram viewer</name>
  <files>tools/er-editor/static/index.html</files>
  <action>
    Start the server and verify the complete Phase 1 experience in the browser.
    Run: `cd tools/er-editor && python server.py tests/fixtures/sample_model.py`
    Browser opens to http://localhost:8000. Verify all visual and interactive requirements per the checklist below.
  </action>
  <verify>
    Human verification of all checklist items below:

    1. Start the server:
       ```
       cd tools/er-editor
       python server.py tests/fixtures/sample_model.py
       ```
    2. Browser opens to http://localhost:8000

    **Visual checks (VIS-01):**
    - [ ] Tables render as boxes: dark gray header with "ClassName (schema.table_name)" in white monospace text
    - [ ] Columns show: PK icon (gold key), FK icon (blue link), name, type, NN/UQ/IDX badges
    - [ ] Mailbox table has: id (PK, Integer), email_address (String, NN), display_name (String), is_active (Boolean)
    - [ ] Relationships section below divider line: "tickets -> EmailTicket[]" in Mailbox

    **Relationship lines (VIS-02):**
    - [ ] Orthogonal lines with 90-degree turns connect related tables
    - [ ] "1" label near parent table, "N" label near child table
    - [ ] No labels on lines (only cardinality markers)

    **Interactions (VIS-03, VIS-04):**
    - [ ] Drag a table: it moves, snaps to grid (hold Alt for free drag)
    - [ ] Scroll wheel: zoom in/out centered on cursor
    - [ ] Drag blank canvas: pans the view
    - [ ] Click table: blue border, connected tables get dashed border, unrelated tables dim
    - [ ] Click background: everything restored
    - [ ] Hover relationship line: line turns blue, both endpoints highlighted
    - [ ] Dot grid visible in background

    **Persistence (PERS-02, PERS-03):**
    - [ ] Move tables to new positions, wait 1 second
    - [ ] Reload page (Cmd+R): tables appear at saved positions (not re-laid-out)
    - [ ] Check that .er.json file exists next to tests/fixtures/sample_model.py

    **Auto-layout (UX-03):**
    - [ ] Delete the .er.json file manually, reload page: tables auto-arranged top-to-bottom
    - [ ] Click "Re-layout" in toolbar: tables re-arranged

    **Toolbar:**
    - [ ] "Fit All" button: zooms to show all tables
    - [ ] "Zoom +" and "Zoom -": change zoom level, percentage updates
    - [ ] "Collapse All": all tables collapse to PK/FK + relationships
    - [ ] "Expand All": all tables expand to show all columns
    - [ ] Double-click a table: toggles its collapse state individually
    - [ ] Search "ticket": EmailTicket highlighted, others dimmed, canvas pans to it
    - [ ] Clear search: all tables restored

    **Browser tab:**
    - [ ] Tab title shows "ER Editor - sample_model.py"
  </verify>
  <done>All visual and interactive checklist items pass. User confirms the ER diagram viewer renders correctly, interactions work as specified, positions persist, and auto-layout functions properly.</done>
</task>

</tasks>

<verification>
Human verification covers all visual and interactive Phase 1 requirements that cannot be tested automatically.
</verification>

<success_criteria>
All checkbox items in the verification task pass. User confirms the ER diagram viewer works correctly for the sample LAIF model.
</success_criteria>

<output>
After completion, create `.planning/phases/01-parser-and-visual-canvas/01-05-SUMMARY.md`
</output>
